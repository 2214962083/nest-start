// 其他 key

// user token 的 请求参数名
export const USER_TOKEN = 'user-token'

// reflect-meta 的 key 列表 ，方便取值

// 路由权限 meta key
export const GUARD_PERMISSIONS = 'guard_permission'

// http 装饰器 successMessage 的 meta key
export const HTTP_DECORATOR_SUCCESS_MESSAGE = 'http_decorator_success_message'

// http 装饰器 errorMessage 的 meta key
export const HTTP_DECORATOR_ERROR_MESSAGE = 'http_decorator_error_message'

// http 装饰器 errorStatus 的 meta key
export const HTTP_DECORATOR_ERROR_STATUS = 'http_decorator_error_status'

// redis key 列表

// redis 登录验证码存储 key
export const REDIS_VERIFICATION_CODE = 'verification_code'
